// Assignment 8: Deferred Shading with Multiple Render Targets (Dynamic Rendering Local Reads)
// Concepts: MRT in dynamic rendering, G-Buffer formats (Albedo, Normal, Depth), local read input attachments.

#include <vulkan/vulkan.h>
#include <iostream>
#include "vulkan_common.hpp"

int main() {
    std::cout << "Assignment 8: Deferred Shading with Multiple Render Targets (Dynamic Rendering Local Reads)" << std::endl;
    return 0;
}
